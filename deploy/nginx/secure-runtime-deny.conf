# Include inside the scuola.xcenter.it server block. This remains safe on rollback:
# PHP can still read the legacy files, but nginx never serves them directly.
location ~ ^/(?:JSON(?:-[^/]+)?(?:/|$)|(?:vapidkeys|subscriptions)\.json$) {
    return 404;
}

